package models.match;

import models.Account;

public class CaptureTheFlagMatch extends Match {
    public CaptureTheFlagMatch(Account account1, Account account2) {
        super(account1, account2);
    }
}
